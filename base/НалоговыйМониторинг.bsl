
&НаКлиенте
Процедура НалоговыйМониторингНажатие(Кнопка)
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("АдресСтраницы", context_params.ApiUrl + "/page/import-taxmon");
	ПараметрыФормы.Вставить("Заголовок", "Налоговый мониторинг");
	ФормаБраузера = ФормаОсновнойОбработки("Browser", ПутьКФормамОбработки(), ПараметрыФормы, ЭтаФорма);
	ФормаБраузера.Открыть();
КонецПроцедуры

