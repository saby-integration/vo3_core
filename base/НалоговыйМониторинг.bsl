
&НаКлиенте
Процедура НалоговыйМониторингНажатие(Кнопка)
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("АдресСтраницы", context_params.api_url + "/page/import-taxmon");
	ПараметрыФормы.Вставить("Заголовок", "Налоговый мониторинг");
	ФормаБраузера = ПолучитьФормуОбработки("Browser", ПутьКФормамОбработки(), ПараметрыФормы, ЭтаФорма);
	ФормаБраузера.Открыть();
КонецПроцедуры

