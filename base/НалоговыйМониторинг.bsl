
&НаКлиенте
Процедура НалоговыйМониторингНажатие(Кнопка)
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("АдресСтраницы", context_param.api_url + "/page/import-taxmon");
	ФормаБраузера = ПолучитьФормуОбработки("Browser", ПутьКФормамОбработки(), ПараметрыФормы, ЭтаФорма);
	ФормаБраузера.Открыть();
КонецПроцедуры

