
&НаКлиенте
Процедура ПерейтиВВебВерсию(Команда)
	ЭтаФорма.Закрыть();  
	ПарамФормы = Новый Структура;
	ПарамФормы.Вставить("context_params", context_params);
	ФормаБраузера = ФормаОсновнойОбработки("Browser", ПутьКФормамОбработки(), ПарамФормы);
	ФормаБраузера.context_params = context_params;
	ФормаБраузера.Открыть();
КонецПроцедуры

