
&НаКлиенте
Процедура ОткрытьФормуАвторизацииПриОтсутствииСессии(context_params, ТранспортИнтеграции, ОбработчикПослеАвторизации) Экспорт
	session = ТранспортИнтеграции.СвойствоСессии(context_params, "session");
	Если НЕ ЗначениеЗаполнено(session) Тогда
		ОткрытьФормуОбработки("Вход", , , , ОбработчикПослеАвторизации);
	Иначе
		ВыполнитьSabyОписаниеОповещения(ОбработчикПослеАвторизации, context_params);
	КонецЕсли;
КонецПроцедуры

