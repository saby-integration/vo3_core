
Процедура ПриНачалеРаботыСистемыНаСервере() Экспорт
	Если ТекущемуПользователюМожноРаботатьСРасширением() Тогда
		Обработка = ПолучитьОбъектОбработки();
		Если Обработка = Неопределено Тогда
			Возврат;
		КонецЕсли;
		НомерТекущейВерсии = Обработка.ПриНачалеРаботы();
	КонецЕсли;
	ИнициализацияРегламентныхЗаданий();
КонецПроцедуры

