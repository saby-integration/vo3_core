
&НаКлиенте
Функция ФормаОсновнойОбработки(ИмяФормыОбработки, ПутьКФормам, ПарамФормы = Неопределено, ВладелецФормы = Неопределено, Уникальность = Неопределено)
	// BSLLS:GetFormMethod-off
	Возврат ПолучитьФорму(ПутьКФормам + ИмяФормыОбработки, ПарамФормы, ВладелецФормы, Уникальность);
	// BSLLS:GetFormMethod-on
КонецФункции
