
&НаКлиенте
Функция ПолучитьПрямуюССылку(СсылкаIE)
	ЗадачаСсылка = СтрЗаменить(СсылкаIE, "://ie-1c.", "://online.");
	ЗадачаСсылка = СтрЗаменить(ЗадачаСсылка, "://fix-ie-1c.", "://fix-online.");
	ЗадачаСсылка = СтрЗаменить(ЗадачаСсылка, "://test-ie-1c.", "://test-online.");
	ЗадачаСсылка = СтрЗаменить(ЗадачаСсылка, "://pre-test-ie-1c.", "://pre-test-online.");
	Возврат ЗадачаСсылка;
КонецФункции
