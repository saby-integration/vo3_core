
Процедура ОбновлениеОтображения()
	Если ЭтаФорма.ВыполнитьЗакрытиеФормы 
		И ЭтаФорма.Открыта()Тогда
		ЭтаФорма.Закрыть();
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыполненияДействия(Результат) Экспорт
	ЭтаФорма.Закрыть();
КонецПроцедуры

