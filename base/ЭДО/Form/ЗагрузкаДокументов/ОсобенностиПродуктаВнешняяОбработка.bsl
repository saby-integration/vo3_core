
&НаКлиенте
Процедура ЗапуститьINI(Контекст) Экспорт
	ЗапуститьINIФоновымЗаданием("Документы_send", Контекст.ПараметрыВызова, Контекст.context_params,
		ЭтаФорма.ВладелецФормы, "Отправка в SABY");
КонецПроцедуры 


&НаСервере
Функция ЗаполнитьСписокПФПоТблДокументов()
	Возврат Неопределено;
КонецФункции 

&НаКлиенте
Процедура ПослеВыполненияДействия(Результат) Экспорт
	ЭтаФорма.Закрыть();
КонецПроцедуры

#Область include_core_base_ЭДО_Form_ЗагрузкаДокументов_ОсобенностиОбработки
#КонецОбласти
