
&НаКлиенте
Функция МодульФоновогоЗаданияСервер()
	ВЛФормы = ЭтаФорма; 
	Пока Истина Цикл
		Попытка 
			Если ВЛФормы.ФормаЗапускаФоновогоЗадания() = Истина Тогда
				Прервать;
			КонецЕсли
		Исключение
			Если ВЛФормы.ВладелецФормы = Неопределено Тогда
				Прервать;
			КонецЕсли;
			ВЛФормы = ВЛФормы.ВладелецФормы;
		КонецПопытки;
	КонецЦикла;
	Возврат ВЛФормы;
КонецФункции	

#Область include_core_base_ДлительныеОперации_ИмяМодуляДлительныеОперации 
#КонецОбласти
