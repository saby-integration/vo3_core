
&НаСервере
Функция РеквизитИдентификаторПодключения()
	Результат = ЭтаФорма.ИдентификаторПодключения;
	Если Не ЗначениеЗаполнено(Результат) Тогда
		Результат = get_prop(context_params, "ConnectionUuid", Неопределено);
	КонецЕсли;
	Возврат Результат;
КонецФункции

