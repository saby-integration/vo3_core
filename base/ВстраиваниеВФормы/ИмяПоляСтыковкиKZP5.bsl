
Функция ИмяПолеСтыковкиСостоянияСпр()
    Возврат "Ссылка";
КонецФункции

Функция ИмяСтыковкиСостоянияДок()
	// Заглушка, нужна для корректной работы запроса, Сотрудник находится в табличной части
    Возврат "Ссылка";
КонецФункции

Функция ИмяПолеСтыковкиКЭДОСпр()
    Возврат "Ссылка";
КонецФункции

Функция ИмяПолеСтыковкиКЭДОДок()
	// Заглушка, нужна для корректной работы запроса, Сотрудник находится в табличной части
    Возврат "Сотрудник";
КонецФункции
