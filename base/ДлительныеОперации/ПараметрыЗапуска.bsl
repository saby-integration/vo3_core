
Функция ПараметрыОбработкиSaby(context = Неопределено)  
	Результат = Новый Структура;
	Результат.Вставить("Обработка", ИмяОбработки());
	Результат.Вставить("ЭтоВнешняяОбработка", Ложь);
	Возврат Результат;
КонецФункции
